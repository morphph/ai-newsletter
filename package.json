{
  "name": "ai-newsletter-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "preview": "cd frontend && npm run preview",
    "install": "cd frontend && npm install",
    "postinstall": "cd frontend && npm install"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}